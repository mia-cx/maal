# WorkOS AuthKit
WORKOS_API_KEY=""
WORKOS_CLIENT_ID=""
WORKOS_COOKIE_PASSWORD=""
# Configure allowed callback/logout URLs in the WorkOS Dashboard.
# In code, pass request-origin URLs such as `${url.origin}/auth/callback`.

# Local smoke/perf auth fixture. Enable only outside production.
MAAL_SMOKE_AUTH_ENABLED="false"

# Stripe
STRIPE_SECRET_KEY=""
PUBLIC_STRIPE_PUBLISHABLE_KEY=""
# Optional, once we add Stripe webhook handling.
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICING_TABLE_ID=""
STRIPE_PRODUCT_ID=""
# Programmatic no-card household trial length.
MAAL_TRIAL_DAYS="30"
